#define _POSIX_C_SOURCE 200809L

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void) {

  // for get line
  char *buff = NULL;
  size_t buff_size = 0;
  ssize_t nread;

  printf("please enter some number: \n");
  nread = getline(&buff, &buff_size, stdin);
  

  free(buff);
  return 0;
}
